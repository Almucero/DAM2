<Window x:Class="Ejercicio1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ejercicio1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Background="Gray">
    <Grid>
        <TextBox AcceptsReturn="True" TextWrapping="Wrap" SpellCheck.IsEnabled="True" Language="es-ES" TextChanged="TextBox_TextChanged" Background="Gray"/>
    </Grid>
    <!--Ejercicio 1:
    Crea una ventana (gris) dentro de la cual situarás un TextBox que te permita escribir varios párrafos, admitiendo saltos de línea,
    A medida que se escribe en el TextBox, automáticamente se usan las líneas necesarias (el texto no se corta, salta a la siguiente línea).
    Además, configura el TextBox de forma que se active automáticamente el corrector ortográfico en Castellano. Escribe el texto y comprueba que el TextBox funcioa tal y como se ha solicitado.-->
</Window>
